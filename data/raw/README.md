# Source data

Place the supplied source workbook in this directory as `UseCase - Airlines.xlsx` before running the pipeline. The source file contains sensitive passenger fields and is intentionally excluded from version control.
